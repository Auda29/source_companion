{
  "name": "source-companion",
  "version": "0.1.3",
  "private": true,
  "scripts": {
    "test": "node --preserve-symlinks --preserve-symlinks-main --test tests/*.test.js",
    "ci:node": "npm run desktop:assets && npm test",
    "desktop:assets": "node --preserve-symlinks --preserve-symlinks-main scripts/copy-desktop-assets.js",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
